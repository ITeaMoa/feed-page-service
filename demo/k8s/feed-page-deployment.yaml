apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedpage-service-deployment
  namespace: feed-local-namespace  # Match the Fargate namespace
  labels:
    node: local
spec:
  replicas: 1  # Two pods running the service
  selector:
    matchLabels:
      node: local
  template:
    metadata:
      labels:
        node: local
    spec:
      containers:
      - name: feedpage-service
        image: feed-page-local-1:latest #<AWS_ACCOUNT_ID>.dkr.ecr.<AWS_DEFAULT_REGION>.amazonaws.com/iteamoa/mainpage:latest # Use ECR
        imagePullPolicy: Never
        ports:
        - containerPort: 8080  # Check container's exposed port 
        env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: AWS_SECRET_ACCESS_KEY